---
- name: Install common packages
  become: true
  dnf:
    name:
      - git
      - wget
      - curl
      - unzip
    state: present

- name: Ensure chronyd is running
  become: true
  service:
    name: chronyd
    state: started
    enabled: true
